当前项目的代码索引配置的索引行为配置中只支持选择是否手动更新、是否自动更新，缺乏项目级的配置。分析该如何添加允许代码索引的项目的列表的配置，ui设计可以参考现有的命令批准设置界面的ui组件。不过由于项目路径一般较长，建议每个项目独占一行。建议不要支持左右滚动，而是悬浮时显示完整路径。
具体作用：每次索引执行前的判断逻辑中都添加对当前项目路径(从vscode的工作路径获取，可以查看其他现有模块的设计)是否在列表中，只有在其中才会开始索引，否则记录日志。
在列表配置最上方添加便捷控件，用于将当前项目添加到列表/从列表删除(条件显示，视当前项目是否在列表中决定显示哪一个)